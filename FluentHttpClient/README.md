# FS.FluentHttpClient

FS.FluentHttpClient is a lightweight library that offers a fluent, easy-to-use interface for building and sending HTTP requests in .NET applications.

## Features
- **Intuitive fluent API** — Chain calls to build requests in a readable way
- **High performance** — Optimized for fast HTTP operations
- **Extensible design** — Easily customize and extend for your needs
